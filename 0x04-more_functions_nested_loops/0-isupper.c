#include "main.h"

/**
 * A function that checks for uppercase character
 * @c: input
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int n)
{
if ((n >= 'A') && (n <= 'Z'))
{
return (1);
}
else

return (0);
}
