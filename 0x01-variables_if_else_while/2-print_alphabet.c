#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Seccuss)
*/
int main(void)
{

char ch;
/* For loop to print the alphabet */
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
putchar('\n');

return (0);
}
